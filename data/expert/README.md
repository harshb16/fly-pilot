# Expert demonstration data

Generated by `ExpertLandingController` (classical autopilot, **not** MaleCNS).

```bash
python -m fly_pilot.record_expert --episodes 50 --seed 0 --output data/expert/demonstrations.parquet
```

Parquet files are gitignored. This directory is the default location for later
imitation-learning work that will pair MaleCNS activity with these actions.
